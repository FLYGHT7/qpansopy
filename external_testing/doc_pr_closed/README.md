This directory tracks Pull Request documentation that is CLOSED (merged or otherwise closed).

Usage:

- Move a PR write-up here once the corresponding PR is merged/closed.
- Optionally add a short note at the top indicating the merge date and commit/PR link.
