pytest==7.4.4
pytest-cov==4.1.0
numpy==1.24.3
# Flake8 store-compliance guard: tests/integration/test_flake8_compliance.py.
# Keep the version pinned so the baseline count is reproducible across machines.
flake8==7.3.0
# qgis-core==3.28.0  # If QGIS functions are needed
